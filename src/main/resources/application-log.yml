logging:
  file:
    name: ./logs/avocado-dev.log

  level:
    org.hibernate.SQL: info
    root: info
    com.zaxxer.hikari.pool.HikariPool: debug

  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7